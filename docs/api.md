# API roadmap

The current HTTP router serves server-rendered pages. A versioned `/api/v1` boundary should be added for mobile clients and integrations with token authentication, request IDs, pagination and idempotency keys.
