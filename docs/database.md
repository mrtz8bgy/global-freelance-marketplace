# Database

Import `database/migrations/001_initial_schema.sql`. All monetary values use DECIMAL, primary entities use unsigned BIGINT, and foreign keys enforce ownership. Add future migrations with monotonically increasing filenames.
